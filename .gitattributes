# Never convert line endings or otherwise touch file bytes.
# Every .rdf is stored as UTF-8 with a BOM and CRLF line endings, and RePEc
# harvests the files byte-for-byte; git must not normalize them on checkout
# or commit.
* -text
